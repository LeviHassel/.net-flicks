@model CoreTemplate.ViewModels.Job.JobsViewModel

@{
    ViewBag.Title = "Jobs";
}

<div class="btn-toolbar float-right">
    @Html.ActionLink("New", "Edit", "Job", null, new { @class = "btn btn-primary" })
</div>
<h1>@ViewBag.Title</h1>

<br /><br />

<div>
    <table class="table sortable">
        <thead>
            <tr>
                <th data-defaultsort="asc"><h5>@Html.DisplayNameFor(x => Model.Jobs.FirstOrDefault().Name)</h5></th>
                <th data-defaultsort="disabled"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var i in Model.Jobs)
            {
                <tr>
                    <td style="vertical-align: middle">@Html.DisplayFor(x => i.Name)</td>
                    <td>
                        <div class="btn-toolbar float-right">
                            <a class="btn btn-primary" style="margin-left: 10px" asp-area="" asp-controller="Job" asp-action="Edit" asp-route-id="@i.Id">
                                <i class="fas fa-edit"></i>
                            </a>
                            <a class="btn btn-primary" style="margin-left: 10px" asp-area="" asp-controller="Job" asp-action="Delete" asp-route-id="@i.Id">
                                <i class="fas fa-trash"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
