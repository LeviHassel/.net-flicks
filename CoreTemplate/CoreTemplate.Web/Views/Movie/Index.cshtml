@model CoreTemplate.ViewModels.Movie.MoviesViewModel

@{
    ViewBag.Title = "Movies";
}

<div class="btn-toolbar float-right">
    @Html.ActionLink("New", "Edit", "Movie", null, new { @class = "btn btn-primary" })
</div>
<h1>@ViewBag.Title</h1>

<br /><br />

<div>
    <table class="table sortable">
        <thead>
            <tr>
                <th data-defaultsort="asc"><h5>@Html.DisplayNameFor(x => Model.Movies.FirstOrDefault().Name)</h5></th>
                <th><h5>@Html.DisplayNameFor(x => Model.Movies.FirstOrDefault().ReleaseDate)</h5></th>
                <th><h5>@Html.DisplayNameFor(x => Model.Movies.FirstOrDefault().Runtime)</h5></th>
                <th><h5>@Html.DisplayNameFor(x => Model.Movies.FirstOrDefault().PurchaseCost)</h5></th>
                <th><h5>@Html.DisplayNameFor(x => Model.Movies.FirstOrDefault().RentCost)</h5></th>
                <th><h5>Image</h5></th>
                <th data-defaultsort="disabled"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var i in Model.Movies)
            {
                <tr>
                    <td style="vertical-align: middle">@Html.DisplayFor(x => i.Name)</td>
                    <td data-dateformat="MM-DD-YYYY" style="vertical-align: middle">@Html.DisplayFor(x => i.ReleaseDate)</td>
                    <td style="vertical-align: middle">@Html.DisplayFor(x => i.Runtime)</td>
                    <td style="vertical-align: middle">@Html.DisplayFor(x => i.PurchaseCost)</td>
                    <td style="vertical-align: middle">@Html.DisplayFor(x => i.RentCost)</td>
                    <td style="vertical-align: middle" data-value=@(!string.IsNullOrEmpty(i.ImageUrl))>
                        @if (!string.IsNullOrEmpty(i.ImageUrl))
                        {
                            <i class="fas fa-image"></i>
                        }
                    </td>
                    <td>
                        <div class="btn-toolbar float-right">
                            <a class="btn btn-primary" style="margin-left: 10px" asp-area="" asp-controller="Movie" asp-action="Edit" asp-route-id="@i.Id">
                                <i class="fas fa-edit"></i>
                            </a>
                            <a class="btn btn-primary" style="margin-left: 10px" asp-area="" asp-controller="Movie" asp-action="Delete" asp-route-id="@i.Id">
                                <i class="fas fa-trash"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
