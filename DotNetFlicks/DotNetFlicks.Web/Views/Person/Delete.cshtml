@model DotNetFlicks.ViewModels.Person.PersonViewModel

@{
    ViewData["Title"] = "Delete " + Model.Name;
}

<h1>@ViewData["Title"]</h1>
<p class="lead">Are you sure you want to delete @Model.Name?</p>
<br /><br />

<form asp-controller="Person" asp-action="Delete" method="post" class="form-horizontal" role="form">
    <div class="container form-actions no-color">
        @if (Model.Roles.Count > 0)
        {
            <h4 class="text-danger">@Model.Name cannot be deleted because they're in the cast or crew for @Model.Roles.Count movies: </h4>
            <p>@Html.Raw(Model.RolesBulletedList)</p>
        }
        else
        {
            <input type="submit" value="Delete" class="btn btn-danger" />
        }
        <br /><br />
        <a asp-action="Index">Back to List</a>
    </div>
</form>